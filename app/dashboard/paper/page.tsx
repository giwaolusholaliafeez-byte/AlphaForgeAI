import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
import { getCurrentPrices } from "@/lib/portfolio/prices";
import { calculateAllocation } from "@/lib/portfolio/valuation";
import type { PortfolioHolding } from "@/types/portfolio";
import ResetPaperAccountButton from "@/components/paper/ResetPaperAccountButton";
import PaperPerformanceChart from "@/components/paper/PaperPerformanceChart";

export default async function PaperPage() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/sign-in");
  const [{ data: account }, { data: positions }, { data: orders }, { data: snapshots }] = await Promise.all([
    supabase.from("paper_accounts").select("cash_balance,starting_balance").eq("user_id", user.id).maybeSingle(),
    supabase.from("paper_positions").select("id,asset_type,asset_id,symbol,quantity,average_cost,realized_pnl").eq("user_id", user.id).order("symbol"),
    supabase.from("paper_orders").select("id,symbol,side,quantity,execution_price,gross_amount,realized_pnl,status,created_at").eq("user_id", user.id).order("created_at", { ascending: false }).limit(100),
    supabase.from("paper_equity_snapshots").select("equity,recorded_at").eq("user_id", user.id).order("recorded_at", { ascending: true }).limit(100),
  ]);
  const holdings: PortfolioHolding[] = (positions ?? []).map((position) => ({ id: position.id, portfolioId: "paper", userId: user.id, assetType: position.asset_type, assetId: position.asset_id, symbol: position.symbol, assetName: position.symbol, quantity: Number(position.quantity), averageCost: Number(position.average_cost), acquiredAt: null, notes: null, createdAt: "", updatedAt: "" }));
  const priceMap = await getCurrentPrices(holdings);
  const valued = holdings.map((holding) => { const currentPrice = priceMap.get(holding.id) ?? null; const marketValue = currentPrice === null ? null : holding.quantity * currentPrice; const costBasis = holding.quantity * holding.averageCost; return { holding, currentPrice, marketValue, costBasis, unrealizedGain: marketValue === null ? null : marketValue - costBasis, returnPercentage: marketValue === null || costBasis === 0 ? null : ((marketValue - costBasis) / costBasis) * 100, allocationPercentage: 0 }; });
  const cash = Number(account?.cash_balance ?? 0);
  const investedValue = valued.reduce((sum, position) => sum + (position.marketValue ?? 0), 0);
  const totalValue = cash + investedValue;
  const realizedPnl = (orders ?? []).reduce((sum, order) => sum + Number(order.realized_pnl ?? 0), 0);
  const unrealizedPnl = valued.reduce((sum, position) => sum + (position.unrealizedGain ?? 0), 0);
  const allocation = calculateAllocation(valued);
  const totalReturn = account?.starting_balance ? ((totalValue - Number(account.starting_balance)) / Number(account.starting_balance)) * 100 : null;
  const chartData = (snapshots ?? []).map((snapshot) => ({ date: new Date(snapshot.recorded_at).toLocaleDateString(undefined, { month: "short", day: "numeric" }), value: Number(snapshot.equity) }));
  return <div className="space-y-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs uppercase tracking-wider text-[#00C2A8]">Paper · Virtual · Simulated</p><h1 className="mt-1 text-2xl font-semibold text-white">Paper portfolio</h1><p className="mt-1 text-sm text-[#A1A7B3]">Practice with virtual cash. No real money or securities are involved.</p></div><ResetPaperAccountButton /></div><div className="grid grid-cols-2 gap-3 lg:grid-cols-6"><Metric label="Portfolio value" value={totalValue} /><Metric label="Buying power" value={cash} /><Metric label="Invested value" value={investedValue} /><Metric label="Unrealized P/L" value={unrealizedPnl} tone={unrealizedPnl >= 0 ? "positive" : "negative"} /><Metric label="Realized P/L" value={realizedPnl} tone={realizedPnl >= 0 ? "positive" : "negative"} /><Metric label="Total return" value={totalReturn ?? 0} suffix="%" tone={totalReturn === null || totalReturn >= 0 ? "positive" : "negative"} /></div><div className="grid gap-4 lg:grid-cols-[1.4fr_1fr]"><PaperPerformanceChart data={chartData} /><section className="rounded-xl border border-white/[0.06] bg-white/[0.02] p-5"><div className="flex items-center justify-between"><h2 className="font-medium text-white">Position allocation</h2><span className="text-[10px] uppercase tracking-wider text-[#00C2A8]">Paper money</span></div>{allocation.length ? <div className="mt-4 space-y-3">{allocation.map((item) => <div key={item.assetType}><div className="flex justify-between text-sm"><span className="capitalize text-[#A1A7B3]">{item.assetType}</span><span className="text-white">{item.percentage.toFixed(1)}%</span></div><div className="mt-1 h-2 rounded-full bg-white/[0.06]"><div className="h-2 rounded-full" style={{ width: `${item.percentage}%`, backgroundColor: item.color }} /></div></div>)}</div> : <p className="mt-4 text-sm text-[#A1A7B3]">Allocation appears after your first paper trade.</p>}</section></div><section className="rounded-xl border border-white/[0.06] bg-white/[0.02] p-5"><h2 className="font-medium text-white">Positions</h2>{valued.length ? <div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{valued.map((position) => <div key={position.holding.id} className="rounded-lg border border-white/[0.06] bg-[#0B0F1A] p-4"><div className="flex justify-between"><span className="font-medium text-white">{position.holding.symbol}</span><span className="text-xs text-[#A1A7B3]">{position.holding.quantity.toLocaleString()} units</span></div><div className="mt-3 grid grid-cols-2 gap-3 text-sm"><Value label="Avg entry" value={`$${position.holding.averageCost.toFixed(2)}`} /><Value label="Current" value={position.currentPrice === null ? "Unavailable" : `$${position.currentPrice.toFixed(2)}`} /><Value label="Market value" value={position.marketValue === null ? "Unavailable" : `$${position.marketValue.toLocaleString(undefined, { maximumFractionDigits: 2 })}`} /><Value label="Gain/loss" value={position.unrealizedGain === null ? "Unavailable" : `$${position.unrealizedGain.toLocaleString(undefined, { maximumFractionDigits: 2 })}`} /></div></div>)}</div> : <p className="mt-4 text-sm text-[#A1A7B3]">No paper positions yet. Use Paper Buy from an asset detail page.</p>}</section><section className="rounded-xl border border-white/[0.06] bg-white/[0.02] p-5"><h2 className="font-medium text-white">Trade history</h2>{orders?.length ? <div className="mt-4 divide-y divide-white/[0.06]">{orders.map((order) => <div key={order.id} className="flex flex-wrap items-center justify-between gap-2 py-3 text-sm"><span className="text-white"><span className={order.side === "buy" ? "text-[#00C2A8]" : "text-red-400"}>{String(order.side).toUpperCase()}</span> {order.quantity} {order.symbol} · ${Number(order.execution_price).toFixed(2)}</span><span className="text-right text-[#A1A7B3]">${Number(order.gross_amount).toLocaleString(undefined, { maximumFractionDigits: 2 })} · {Number(order.realized_pnl ?? 0) !== 0 ? `P/L $${Number(order.realized_pnl).toFixed(2)} · ` : ""}{String(order.status).toUpperCase()}<br /><span className="text-xs text-[#64748B]">{new Date(order.created_at).toLocaleString()}</span></span></div>)}</div> : <p className="mt-4 text-sm text-[#A1A7B3]">No paper trades yet.</p>}</section></div>;
}

function Metric({ label, value, tone, suffix = "$" }: { label: string; value: number; tone?: "positive" | "negative"; suffix?: string }) { return <div className="rounded-xl border border-white/[0.06] bg-white/[0.02] p-4"><p className="text-[11px] text-[#A1A7B3]">{label}</p><p className={`mt-2 text-lg font-semibold tabular-nums ${tone === "positive" ? "text-[#00C2A8]" : tone === "negative" ? "text-red-400" : "text-white"}`}>{suffix === "$" ? "$" : ""}{value.toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}{suffix === "%" ? "%" : ""}</p></div>; }
function Value({ label, value }: { label: string; value: string }) { return <div><p className="text-xs text-[#64748B]">{label}</p><p className="text-white">{value}</p></div>; }

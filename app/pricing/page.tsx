import Link from 'next/link';
import { Button } from '@/components/ui/button';

export default function PricingPage() { return <main className="min-h-screen bg-[#0B0F1A] p-6 text-white"><div className="mx-auto max-w-5xl py-16"><p className="text-xs uppercase tracking-[0.2em] text-[#00C2A8]">AlphaForge plans</p><h1 className="mt-3 text-4xl font-semibold">Simple access for every market journey.</h1><div className="mt-10 grid gap-5 md:grid-cols-2"><Plan name="Free" price="$0" features={["Markets, news, and basic research", "1 personal portfolio", "Paper trading with $100,000 virtual cash", "20 watchlist assets and 5 alerts"]} /><Plan name="Pro" price="Paid through Paystack" features={["Unlimited portfolios, watchlists, and alerts", "Advanced analytics and research", "Export features", "Premium intelligence tools as released"]} featured /></div><Button asChild variant="ghost" className="mt-8"><Link href="/sign-up">Create an account</Link></Button></div></main>; }
function Plan({ name, price, features, featured = false }: { name: string; price: string; features: string[]; featured?: boolean }) { return <section className={`rounded-2xl border p-7 ${featured ? 'border-[#2563EB] bg-[#2563EB]/10' : 'border-white/[0.08] bg-white/[0.02]'}`}><div className="flex items-center justify-between"><h2 className="text-2xl font-medium">{name}</h2>{featured && <span className="text-xs uppercase tracking-wider text-[#60A5FA]">Recommended</span>}</div><p className="mt-4 text-xl text-[#A1A7B3]">{price}</p><ul className="mt-8 space-y-3 text-sm text-[#CBD5E1]">{features.map((feature) => <li key={feature}>✓ {feature}</li>)}</ul></section>; }
